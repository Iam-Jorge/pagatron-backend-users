export class Friendship {
    constructor(id, user_id, friend_id, status) {
      this.id = id;
      this.user_id = user_id;
      this.friend_id = friend_id;
      this.status = status;
    }
  }
  